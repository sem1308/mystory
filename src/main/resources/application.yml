spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mystory_db?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&zeroDateTimeBehavior=convertToNull
    username: root
    password: 1308
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        #        show_sql : true # sout?? ??
        # format_sql: true # ??? ?? ?? ??